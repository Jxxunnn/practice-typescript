function 함수5(param) {
    if (typeof param === "string") {
        console.log(param);
    }
    else {
        console.log(param);
    }
}
var 함수11 = fucntion(), _a = void 0,  = _a.throw, Error = _a.new;
();
